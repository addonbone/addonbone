import {defineConfig} from "adnbn";

export default defineConfig({
    name: "Offscreen Service Integration",
    description: "MV3 fixture for the offscreen-to-background service transport.",
    jsFilename: "[name].js",
});
